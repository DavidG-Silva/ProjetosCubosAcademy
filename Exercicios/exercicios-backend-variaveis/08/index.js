
let raioCirculo = 1;

let comprimentoCirculo;
let areaCirculo;

comprimentoCirculo = 2 * Math.PI * raioCirculo;

areaCirculo = Math.PI * (raioCirculo ** 2);

console.log(`Para um circulo com raio igual a ${raioCirculo}, o comprimento tem valor ${comprimentoCirculo.toFixed(1)} 
e a area tem valor ${areaCirculo.toFixed(2)}`);
